var React = require('react'),
    Router = require('react-router');

var { RouteHandler } = Router;

var Body = React.createClass({
  render: () => {
    return (
      <h1>Body</h1>
    )
  }
});

module.exports = Body;
